{
  "name": "next13",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=dev next dev -p 3001",
    "build:uat": "cross-env NODE_ENV=uat next build",
    "build": "cross-env NODE_ENV=production next build",
    "build:prod": "cross-env NODE_ENV=production next build",
    "start": "cross-env NODE_ENV=production next build && next start -p 3001",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "14.0.2",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "cross-env": "^7.0.3",
    "eslint": "^8",
    "eslint-config-next": "14.0.2",
    "typescript": "^5"
  }
}
